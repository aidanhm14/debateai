{
  "name": "debatable-e2e",
  "private": true,
  "type": "module",
  "description": "Browser smoke tests for itsdebatable.com. Runs against production after every deploy.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "save-auth": "node save-auth.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
