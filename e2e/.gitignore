node_modules/
test-results/
playwright-report/
# Saved Google sessions for the two-person spar test. The repo is public;
# a storage state IS a signed-in account. Never commit one.
auth/

# Reproducible Astra audit captures, written locally by the opt-in harnesses.
astra-output/
astra-onboarding-output/
