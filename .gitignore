
# netlify dev build artifacts
.netlify/
deno.lock

# Counter chrome extension build artifacts
app/extension/dist/
app/extension/.stage/

# Counter chrome extension local OAuth key
app/extension/.local/

# secrets. Nothing's ever been committed; keep it that way now the repo is public.
.env
.env.*
secrets.json
service-account*.json
firebase-adminsdk-*.json
credentials.json
*.pem
*.key

# OS / editor noise
.DS_Store
.vscode/
.idea/

# node
node_modules/
