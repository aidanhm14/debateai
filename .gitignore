
# netlify dev build artifacts
.netlify/
deno.lock

# Counter chrome extension build artifacts
app/extension/dist/
app/extension/.stage/

# Counter chrome extension local OAuth key
app/extension/.local/

# secrets. Nothing's ever been committed; keep it that way now the repo is public.
.env
.env.*
secrets.json
service-account*.json
firebase-adminsdk-*.json
credentials.json
*.pem
*.key
*.token
.discord-bot-token
.claude.json
.claude.json.*
.npmrc
.ssh/
.zsh_history

# personal home-directory contents (repo root is ~, keep git out of personal Mac)
Library/
Pictures/
Music/
Movies/
Desktop/
Documents/
Downloads/
Applications/
Public/
Sites/
Zotero/
.Trash/
.cups/
.config/
.local/
.npm/
.npm-global/
.oracle_jre_usage/
.viminfo
.CFUserTextEncoding
.zsh_sessions/
.cache/

# OS / editor noise
.DS_Store
.vscode/
.idea/

# node
node_modules/

# generated private eval/fine-tune artifacts
scripts/eval/out/
.adjudication-finetune/

# Cofounder handoff packet — accounts, access, env vars. Private, never commit.
HANDOFF/

# Pitch deck editors — embed the investor deck (raise, valuation, financials).
debateit-deck/
debatable-deck/

# Local rescue backups made when re-syncing this checkout
.local-tree-backup-*/

# personal dotfiles / tool state (repo root is ~, keep git out of personal Mac)
.zshrc
.zshrc.bak.*
.zsh_sessions/
.zcompdump*
.gitconfig
.netrc
.aws/
.gem/
.grok/
.agents/
.mcp-auth/
.cocoapods/
.swiftpm/
.debateai-editor/
.tracker.prefs
